name: planmexico
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0
  record: ^6.1.2
  audioplayers: ^6.5.1
  permission_handler: ^12.0.1
  path_provider: ^2.1.5
  speech_to_text: ^7.3.0
  avatar_glow: ^3.0.0 # Para la animación de ondas del micrófono
  google_fonts: ^6.1.0
  url_launcher: ^6.3.2
  cached_network_image: ^3.4.1
  share_plus: ^10.1.4
  shared_preferences: ^2.5.3
  supabase: ^2.10.0
  supabase_flutter: ^2.10.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/images/ajolotito.png
    - assets/images/Fondo.png
    - assets/images/ajolote.gif

      